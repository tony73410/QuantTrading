# Source Code

所有正式程序代码放在本目录。当前正式模块为 `quant_trading.market_history`，提供股票历史行情的本地缓存和桌面浏览，不包含交易功能。

模块采用 `src` package layout，通过 `pyproject.toml` 安装。新增模块仍须遵循 `docs/modules/README.md` 的审批与文档要求，并更新模块映射、测试、项目状态和编辑日志。
